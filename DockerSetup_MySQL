Docker MySQL Setup

Create volume:
docker volume create **VOLUME_NAME**
docker volume create Capstone_dev

Pull most recent MySQL image:
docker run -d --name **CONTAINER_NAME** -e MYSQL_ROOT_PASSWORD=**ROOT_PASSWORD** -v **VOLUME_NAME**:/var/lib/mysql -p **PORT** mysql:latest
docker run -d --name Capstone_dev -e MYSQL_ROOT_PASSWORD=capstone_dev -v Capstone_dev:/var/lib/mysql -p 3306:3306 mysql:latest